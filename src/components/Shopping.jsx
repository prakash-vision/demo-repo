import React, { Fragment } from "react";
import { Outlet } from "react-router-dom";

const Shopping = () => {
  return (
    <Fragment>
      <Outlet />
    </Fragment>
  );
};

export default Shopping;
